
services:
  web:
    container_name: aura-shop-web
    build: .
    ports:
      - "1111:80"
    volumes:
      - .:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: always